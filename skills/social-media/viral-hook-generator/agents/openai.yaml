interface:
  display_name: "Viral Hook Generator"
  short_description: "Create platform-native social hooks"
  default_prompt: "Use $viral-hook-generator to create platform-native hooks for this campaign."

policy:
  allow_implicit_invocation: true
